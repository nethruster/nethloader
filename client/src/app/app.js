import { h, Component } from 'preact';

import style from './app.scss';

export default class App extends Component {
    render() {
        return (
            <div>
                <h1>Nethloader</h1>
            </div>
        );
    }
}
