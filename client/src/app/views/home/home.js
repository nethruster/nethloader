import { h, Component } from 'preact';

import style from './home.scss';

export default class Home extends Component {
    render() {
        return (
            <div>
                <h1>Default, not logged in and not in imageview view</h1>
            </div>
        );
    }
}
